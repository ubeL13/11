#include <gtest/gtest.h>

#include <functions/functions.h>

using namespace employee;

TEST(EmployeeTests, FullTimeSallary){
    const auto base1 = Employee::full_t(20000);
    const auto base2 = Employee::full_t(30000);
    const auto exp1 = Employee::full_t(1);
    const auto exp2 = Employee::full_t(2);
   
   
}



